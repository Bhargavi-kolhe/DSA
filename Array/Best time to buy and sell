class Solution {
    public int maxProfit(int[] prices) {
        int minPrice=a[0];
        int maxProfit=0;

        for(int i=1;i<prices.length;i++){
            if(arr[i]<minPrice){
                minPrice=a[i];
            }
            int profit = arr[i]-minPrice;

            if(profit > maxProfit){
                maxProfit = profit;
            }
        }
        return maxProfit;

        
    }
}
