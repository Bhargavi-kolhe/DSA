class Solution {
    public boolean isSorted(ArrayList<Integer> nums) {
        for(int i = 0; i<nums.length-1 ;i++){
            if(nums[i]>nums[i+1]){
                 return false;
            }
           

            } 
            return true;
        }
        //your code goes here
    }
